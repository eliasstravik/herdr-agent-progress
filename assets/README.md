# README assets

The badge, CTA, and sidebar illustration are local SVGs with accessible titles. The sidebar is explicitly an illustration, not a screenshot or live-test evidence. It shows the plugin's actual summary syntax and keeps native lifecycle state separate from the task estimate.

Live verification captures belong in the pull request. No private sessions, task names, or credentials belong in these reusable product assets.

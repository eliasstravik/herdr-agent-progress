# README assets

The README uses the same Shields flat-square badge, 40px CTA style, and 960 × 500 illustration frame as Herdr Call. The CTA and illustration are local SVGs with accessible titles. The illustration follows a task through the agent's report to the sidebar, using the plugin's actual summary syntax. It is not a screenshot or live-test evidence.

Live verification captures belong in the pull request. No private sessions, task names, or credentials belong in these reusable product assets.
